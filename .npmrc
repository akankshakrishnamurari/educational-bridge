registry=https://registry.npmjs.org/
# Material-UI v4 declares peer deps on React 16/17; this project runs React 18.
# Everything works fine in practice, but npm's strict resolver (used by both
# `npm ci` and Amplify's build) refuses to install without this flag.
legacy-peer-deps=true
